# Университетский календарь (University Calendar)

Веб-приложение коллективного календаря для студентов университета с возможностью управления расписанием занятий, отслеживания дедлайнов и совместной работы с файлами.

## Особенности

### 🗓️ Управление календарем
- Просмотр в режимах: месяц, неделя, день
- Создание, редактирование и удаление событий
- Интуитивная навигация по датам
- Поиск событий по названию, курсу, типу

### 📚 Типы событий
- **Расписание занятий**: регулярные еженедельные пары
- **Домашние задания**: дедлайны с указанием приоритета
- **Экзамены**: даты экзаменов и зачетов
- **Прочие события**: дополнительные академические мероприятия

### 📎 Работа с файлами
- Прикрепление файлов к событиям (до 50 МБ)
- Поддержка форматов: PDF, DOC, DOCX, JPG, PNG, PPT, PPTX, TXT, ZIP
- Предварительный просмотр и скачивание файлов
- Организация файлов по курсам и типам событий

### 👥 Совместная работа
- Редактирование календаря несколькими пользователями
- Разграничение прав доступа (студент/администратор группы)
- Отображение автора создания/изменения события

### 🔔 Повторяющиеся события
- Настройка еженедельного расписания занятий
- Указание дней недели для повторения
- Установка периода действия расписания

## Технологический стек

### Backend
- **Rust** - основной язык программирования
- **Axum** - веб-фреймворк
- **PostgreSQL** - база данных
- **SQLx** - асинхронный драйвер БД
- **JWT** - аутентификация
- **bcrypt** - хеширование паролей

### Frontend
- **HTML5/CSS3** - разметка и стили
- **Vanilla JavaScript** - интерактивность
- **Font Awesome** - иконки
- **Responsive Design** - адаптивность

## Установка и запуск

### Предварительные требования

1. **Rust** (версия 1.70+)
   ```bash
   curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
   ```

2. **PostgreSQL** (версия 12+)
   - Windows: скачайте с [официального сайта](https://www.postgresql.org/download/windows/)
   - macOS: `brew install postgresql`
   - Linux: `sudo apt-get install postgresql postgresql-contrib`

3. **Git**
   ```bash
   git --version
   ```

### Настройка проекта

1. **Клонирование репозитория**
   ```bash
   git clone <repository-url>
   cd calendar-rs
   ```

2. **Настройка базы данных**
   ```bash
   # Создайте базу данных PostgreSQL
   createdb university_calendar
   
   # Или через psql:
   psql -U postgres
   CREATE DATABASE university_calendar;
   \q
   ```

3. **Настройка переменных окружения**
   ```bash
   # Скопируйте файл примера
   cp .env.example .env
   
   # Отредактируйте .env файл
   nano .env
   ```

   Обновите следующие переменные в `.env`:
   ```env
   DATABASE_URL=postgresql://username:password@localhost:5432/university_calendar
   JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
   HOST=127.0.0.1
   PORT=3000
   UPLOAD_DIR=./uploads
   MAX_FILE_SIZE=52428800
   CORS_ORIGIN=http://localhost:3000
   RUST_LOG=info
   ```

4. **Установка зависимостей и запуск миграций**
   ```bash
   # Установка SQLx CLI (если не установлен)
   cargo install sqlx-cli --no-default-features --features postgres
   
   # Запуск миграций
   sqlx migrate run
   ```

5. **Создание директории для загрузок**
   ```bash
   mkdir uploads
   ```

6. **Сборка и запуск**
   ```bash
   # Режим разработки
   cargo run
   
   # Режим продакшена
   cargo build --release
   ./target/release/calendar-rs
   ```

7. **Открытие приложения**
   Откройте браузер и перейдите по адресу: http://localhost:3000

## Использование

### Первый запуск

1. **Регистрация**
   - Нажмите кнопку "Регистрация"
   - Заполните форму (имя, email, пароль, роль)
   - Выберите роль: "Студент" или "Администратор группы"

2. **Вход в систему**
   - Используйте зарегистрированные данные для входа
   - После входа откроется интерфейс календаря

### Работа с событиями

1. **Создание события**
   - Нажмите "Создать событие" или дважды кликните на дату
   - Заполните обязательные поля: название, тип, время начала и окончания
   - Опционально: описание, курс, преподаватель, место проведения
   - Установите приоритет (низкий/средний/высокий)

2. **Повторяющиеся события**
   - Отметьте чекбокс "Повторяющееся событие"
   - Выберите частоту (еженедельно/ежедневно)
   - Укажите дни недели и дату окончания

3. **Просмотр и редактирование**
   - Кликните на событие для просмотра деталей
   - Используйте кнопки "Редактировать" или "Удалить"

### Работа с файлами

1. **Загрузка файлов**
   - Откройте детали события
   - Нажмите "Загрузить файл"
   - Выберите файл (до 50 МБ)

2. **Скачивание файлов**
   - В деталях события нажмите кнопку скачивания рядом с файлом

### Навигация по календарю

- **Виды отображения**: переключайтесь между месяцем, неделей и днем
- **Навигация**: используйте стрелки для перехода между периодами
- **Сегодня**: быстрый переход к текущей дате
- **Фильтры**: фильтрация по типу событий и поиск

## API Endpoints

### Аутентификация
- `POST /api/auth/register` - регистрация пользователя
- `POST /api/auth/login` - вход в систему
- `GET /api/auth/profile` - получение профиля пользователя

### События
- `GET /api/events` - получение списка событий
- `POST /api/events` - создание события
- `GET /api/events/:id` - получение события по ID
- `PUT /api/events/:id` - обновление события
- `DELETE /api/events/:id` - удаление события

### Файлы
- `POST /api/events/:event_id/files` - загрузка файла к событию
- `GET /api/events/:event_id/files` - получение файлов события
- `GET /api/files/:id` - скачивание файла
- `DELETE /api/files/:id` - удаление файла

### Пользователи
- `GET /api/users` - получение списка пользователей (только для админов)

## Структура проекта

```
calendar-rs/
├── src/
│   ├── handlers/          # API обработчики
│   │   ├── auth.rs       # Аутентификация
│   │   ├── events.rs     # События
│   │   ├── files.rs      # Файлы
│   │   └── mod.rs        # Модуль handlers
│   ├── auth.rs           # JWT и middleware
│   ├── database.rs       # Подключение к БД
│   ├── models.rs         # Модели данных
│   └── main.rs           # Точка входа
├── static/               # Статические файлы
│   ├── index.html        # Главная страница
│   ├── styles.css        # Стили
│   └── app.js           # JavaScript приложение
├── migrations/           # Миграции БД
├── uploads/             # Загруженные файлы
├── Cargo.toml           # Зависимости Rust
├── .env.example         # Пример переменных окружения
├── schema.sql           # Схема базы данных
└── README.md            # Документация
```

## Разработка

### Запуск в режиме разработки

```bash
# Автоматическая перезагрузка при изменениях
cargo install cargo-watch
cargo watch -x run

# Логирование
RUST_LOG=debug cargo run
```

### Тестирование

```bash
# Запуск тестов
cargo test

# Проверка кода
cargo clippy

# Форматирование
cargo fmt
```

### Миграции базы данных

```bash
# Создание новой миграции
sqlx migrate add migration_name

# Применение миграций
sqlx migrate run

# Откат миграции
sqlx migrate revert
```

## Производительность

- Загрузка календаря: < 2 секунд
- Поддержка файлов: до 50 МБ
- Одновременные пользователи: 50+
- Синхронизация: в реальном времени

## Безопасность

- JWT токены для аутентификации
- Хеширование паролей с bcrypt
- Валидация типов файлов
- Разграничение прав доступа
- Защита от SQL инъекций

## Лицензия

MIT License

## Поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте логи приложения
2. Убедитесь, что PostgreSQL запущен
3. Проверьте настройки в `.env` файле
4. Создайте issue в репозитории

## Roadmap

- [ ] WebSocket для real-time синхронизации
- [ ] Система уведомлений
- [ ] Экспорт в iCal/Google Calendar
- [ ] Мобильное приложение
- [ ] Интеграция с мессенджерами
- [ ] Расширенная система ролей
